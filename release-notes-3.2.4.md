# FollowerForge 3.2.4

## Fixes
1. **MO2 switch during load** — "Use Mod Organizer 2 instead" is visible from first paint and works while Vortex is indexing (cancels and restarts; catalogue builds serialised).
2. **False NO SCULPT** — RaceMenu **Export Head** bakes geometry into the `.nif`. Empty jslot `sculpt.data` (common with FaceForge presets) no longer shows NO SCULPT when the head mesh has verified shapes (e.g. Xija).

## Download
`FollowerForge-3.2.4-win-x64.zip` — unzip, run `FollowerForge.exe` (Windows tool, not a Skyrim mod).

Zip: 99154348 bytes  
SHA-256: `9CECB04D96AAB135185B82F6B6296C6A8E61FB718473F08F0B3BA313F6DB94D9`