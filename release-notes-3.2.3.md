# FollowerForge 3.2.3

## Fixes
- No more SavePCFace / missing `.npc` noise on healthy RaceMenu **Export Head** faces
- Head mesh shape count probed at discovery; **zero-shape** heads blocked at import (black-face protection)
- **GUI** sidebar switch: Vortex ↔ Mod Organizer 2 (saved under `%LOCALAPPDATA%\FollowerForge\prefer-mo2`)
- Custom MO2 `overwrite_directory` used for CharGen / plugin resolution
- User-facing CLI hints say `FollowerForge.Cli.exe` (not bare `fforge`)

## Download
`FollowerForge-3.2.3-win-x64.zip` — unzip anywhere, run `FollowerForge.exe` (Windows **tool**, not a Skyrim mod).